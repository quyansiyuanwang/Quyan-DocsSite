name: Quality Check

on:
  pull_request:
    branches: [master]
    types: [ready_for_review, synchronize]

jobs:
  quality:
    runs-on: ubuntu-latest
    if: |
      !contains(github.event.pull_request.title, '[skip ci]') &&
      !contains(github.event.pull_request.title, '[skip cicd]')

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect relevant changes
        id: changes
        uses: dorny/paths-filter@v3
        with:
          filters: |
            relevant:
              - 'src/**'
              - 'scripts/**'
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'tsconfig*.json'
              - 'vite.config.ts'
              - 'openapi-ts.config.ts'
              - 'eslint.config.ts'
              - 'tailwind.config.js'
              - '.github/workflows/quality-check.yml'

      - name: Setup pnpm
        if: steps.changes.outputs.relevant == 'true'
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        if: steps.changes.outputs.relevant == 'true'
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Cache node_modules
        if: steps.changes.outputs.relevant == 'true'
        uses: actions/cache@v3
        id: cache
        with:
          path: node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('pnpm-lock.yaml') }}

      - name: Install dependencies
        if: steps.changes.outputs.relevant == 'true' && steps.cache.outputs.cache-hit != 'true'
        run: pnpm install --frozen-lockfile

      - name: Lint
        if: steps.changes.outputs.relevant == 'true'
        run: pnpm run lint:check

      - name: Format check
        if: steps.changes.outputs.relevant == 'true'
        run: pnpm run format:check

      - name: Type check
        if: steps.changes.outputs.relevant == 'true'
        run: pnpm run type-check

      - name: Skip quality check
        if: steps.changes.outputs.relevant != 'true'
        run: echo "No relevant docs site changes detected, skipping quality checks."
